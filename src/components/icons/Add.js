import React from 'react';
import icon from '@material-ui/icons/Add';
import BaseMaterialIcon from '../../internal/BaseMaterialIcon';

const Add = props => <BaseMaterialIcon {...props} icon={icon} />;

export default Add;
